# Get aftables match ids

**\[deprecated\]**

All `get_` functions were replaced with `fetch_*` functions. Please use
`fetch_footywire_match_ids()` instead

## Usage

``` r
get_footywire_match_ids(season)
```

## Examples

``` r
#
if (FALSE) { # \dontrun{
get_footywire_match_ids(2020, 1)
# ->
fetch_footywire_match_ids(2020)
} # }
```
