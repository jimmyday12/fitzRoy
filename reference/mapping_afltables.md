# AFL Tables Mapping: AFL Tables Naming Convention Mapping

A dataframe used for mapping names of data columns when scraping match
statistics using the `scrape_afltables_match()` function.

## Usage

``` r
mapping_afltables
```

## Format

An object of class `character` of length 25.
