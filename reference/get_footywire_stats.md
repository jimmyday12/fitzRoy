# Scrape footywire player statistics.

**\[deprecated\]**

All `get_` functions were replaced with `fetch_*` functions. Please use
`fetch_footywire_stats()` instead

## Usage

``` r
get_footywire_stats(ids)
```

## Examples

``` r
#
if (FALSE) { # \dontrun{
get_footywire_stats(5000)
# ->
fetch_footywire_stats(5000)
} # }
```
